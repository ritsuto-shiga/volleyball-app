# バレーボール戦術分析システム - Google Cloud デプロイガイド

## 📋 概要

このアプリは Streamlit で作られたバレーボール戦術分析システムです。
Google Cloud Platform (GCP) を使用してクラウドデプロイを実現しています。

### 🏗️ アーキテクチャ
- **アプリサーバー**: Google Cloud Run (Dockerコンテナ)
- **データ保管**: Google Cloud Storage (バケット: volley-app)
- **ビルド**: Google Cloud Build
- **URL**: https://volley-app-67690434800.asia-northeast1.run.app

## 🚀 導入手順

### ステップ1: Google Cloud SDK のインストール
```bash
# インストーラーをダウンロードして実行
# https://cloud.google.com/sdk/docs/install#windows からダウンロード
# インストール完了後、PowerShell を再起動
```

### ステップ2: gcloud の初期化
```powershell
gcloud init
# ブラウザで認証
# プロジェクト選択: gen-lang-client-0737556836
```

### ステップ3: 請求アカウントの設定
```powershell
# 請求アカウント一覧確認
gcloud beta billing accounts list

# プロジェクトに請求アカウントをリンク
gcloud billing projects link gen-lang-client-0737556836 --billing-account=01AFE5-1ED799-37AB01
```

### ステップ4: Cloud Storage バケット作成
```powershell
# バケット作成
gsutil mb gs://volley-app

# 既存データをアップロード
gsutil -m cp -r users.json data/* gs://volley-app/data/
```

### ステップ5: 依存関係の準備
requirements.txt:
```
streamlit
pandas
google-cloud-storage
matplotlib
```

### ステップ6: コードの修正
volley_app.py に以下の変更を適用:
- Google Cloud Storage のインポート追加
- ファイル操作関数を GCS 対応に変更
- ローカルファイルへのフォールバックを維持

### ステップ7: Docker イメージのビルド
```powershell
gcloud builds submit --tag gcr.io/gen-lang-client-0737556836/volley-app --project=gen-lang-client-0737556836
```

### ステップ8: Cloud Run へのデプロイ
```powershell
gcloud run deploy volley-app --image gcr.io/gen-lang-client-0737556836/volley-app --platform managed --region asia-northeast1 --allow-unauthenticated --project=gen-lang-client-0737556836
```

## 📁 ファイル構造

```
volleyball-v4/
├── volley_app.py          # メインアプリ
├── requirements.txt       # Python依存関係
├── Dockerfile            # コンテナ定義
├── app.yaml              # App Engine用 (未使用)
├── users.json            # ユーザー情報 (GCSに移行)
└── data/                 # データディレクトリ (GCSに移行)
    └── ritsuto/
        ├── config.json
        ├── current_match.jsonl
        ├── current_match_meta.json
        └── matches/
```

## 💾 データ保管場所

### Google Cloud Storage
- バケット: `gs://volley-app`
- 保存データ:
  - `users.json`: ユーザー情報
  - `data/{username}/config.json`: プレイヤー設定
  - `data/{username}/current_match.jsonl`: 試合イベント
  - `data/{username}/current_match_meta.json`: 試合メタ情報
  - `data/{username}/matches/`: 過去試合データ

### 特徴
- 永続的ストレージ
- 複数ユーザー対応
- 自動バックアップ

## 💰 費用目安 (月額)

| サービス | 無料枠 | 想定使用量 | 費用 |
|----------|--------|------------|------|
| Cloud Run | 200万リクエスト | 1,000アクセス | 無料 |
| Cloud Storage | 5GB | 70KB | 無料 |
| Cloud Build | 120分 | 数分 | 無料 |
| **合計** | - | - | **0-10円** |

## 🔧 運用・メンテナンス

### コード更新時
1. コードを修正
2. `gcloud builds submit` でビルド
3. `gcloud run deploy` でデプロイ

### データ管理
- GCS コンソールから直接確認可能
- ローカルバックアップ: `gsutil cp -r gs://volley-app/data/* ./backup/`

### ログ確認
```powershell
gcloud logging read "resource.type=cloud_run_revision AND resource.labels.service_name=volley-app" --limit=10
```

## 🛡️ セキュリティ

- 現在: 公開アクセス可能 (`--allow-unauthenticated`)
- 制限する場合: Cloud Run の IAM 設定で認証必須に変更

## 📞 トラブルシューティング

### よくあるエラー
- `ModuleNotFoundError`: requirements.txt に依存関係を追加して再ビルド
- `AccessDeniedException`: 請求アカウントがリンクされているか確認
- ポートエラー: Dockerfile の EXPOSE が 8080 になっているか確認

### ローカルテスト
```powershell
# ローカルで動作確認
streamlit run volley_app.py --server.port=8501
```

## 📝 メモ

- プロジェクトID: gen-lang-client-0737556836
- バケット名: volley-app
- サービス名: volley-app
- リージョン: asia-northeast1
- 最終デプロイ日: 2026年3月10日

---

このガイドは導入時の手順をまとめたものです。
詳細なコード変更は volley_app.py のコミット履歴を参照してください。